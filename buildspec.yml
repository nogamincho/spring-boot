version: 0.2
phases:
  pre_build:
    commands:
      - echo $JAVA_HOME
      - mvn --version
  build:
    commands:
      - cd spring-boot-samples
      - cd spring-boot-sample-webflux
      - mvn test
      - cd ../
      - cd spring-boot-sample-test
      - mvn test
